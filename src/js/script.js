document.addEventListener('DOMContentLoaded', function(){
    $(".slickSlider").slick({
        // normal options...
        infinite: true,
        slidesToShow: 3,
        dots: false,
        arrows: false,
        autoplay: true,
        autoplaySpeed: 5000,
        // centerPadding: '40px',
    });
});